function negativeOnLeft(arr,left ,right){
  
  while(left<right){
    if(
      arr[low]<0;
      low++
    )
    elseif(arr[high]>0);
    high++;
  }
  else
  arr[low]=arr[high];
}
let arr= 1,2,-3,4,-5,6
let result=negativeOnLeft(arr);
consol.log(result);