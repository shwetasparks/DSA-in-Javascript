/*
The length property returns or sets the number of elements in an array.
*/

let city=["chandigarh","shillong","delhi"];
console.log(city.length);

let laptop=[];
console.log(laptop.length);

//array.length in loop

let colors=["red","green", "yellow"];
for(let i=0;i<colors.length;i++){
  console.log(colors[i]);
}

//changing length property of array
let language=["eng","hindi", "malyali","asamese","punjabi"];
console.log(language.length=6);
console.log(language);
